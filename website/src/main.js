// ── Main Entry Point ────────────────────────────────────────────────
import { renderHome } from "./pages/home.js";

// Initialize: render the home page directly into #app
const app = document.getElementById("app");
renderHome(app);
